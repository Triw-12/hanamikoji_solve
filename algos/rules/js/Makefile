# Add all your champion files in here, separated by spaces
# Include all the sources (*.js) and README files.
CHAMPION_FILES = champion.js

# ----
# Do not modify the lines below

CHAMPION_FILES += interface.cc
-include Makefile-js
